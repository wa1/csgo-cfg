{
  "name": "csgo-config",
  "version": "0.1.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "concat": "concat-glob-cli -f src/_*.cfg -o dist/autoexec.cfg"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concat-glob-cli": "^0.1.0",
    "gulp": "^4.0.0",
    "gulp-concat": "^2.6.1"
  }
}
